# ADR-042 — No direct fetch in UI components

Status: Accepted

## Decision

UI components MUST read data through the `useQuery` hooks layer and MUST NOT issue
network requests directly (no `fetch`, no raw client calls in a component body or
effect). Centralising requests in hooks gives one place for caching, ret/auth, and
error handling.

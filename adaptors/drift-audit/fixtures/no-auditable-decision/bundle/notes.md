# Scratch notes

This bundle was assembled by mistake: it contains a code snippet and some
meeting notes but no ADR clause — there is no decision to audit here.

```ts
export const clamp = (n: number, lo: number, hi: number): number =>
  Math.min(hi, Math.max(lo, n));
```

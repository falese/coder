# Provenance (ADR-keyed, bounded snippets)

- commit a1b9f30 "feat(core): introduce DomainError hierarchy (ADR-017)" —
  "Adds DomainError base + CartEmptyError/PaymentDeclinedError. Follow-up will
  migrate checkout.ts off bare `throw new Error`."
- PR #212 "Enforce ADR-017 in core" (open) — "checkout.ts is the last file still
  throwing plain Errors; blocking the adr-lint rule until it's migrated."

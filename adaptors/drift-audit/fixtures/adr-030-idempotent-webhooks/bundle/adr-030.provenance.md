# Provenance (ADR-keyed, bounded snippets)

- issue #488 "Duplicate ledger entries on Stripe retries (ADR-030)" —
  "We record the charge before the dedupe guard; retried webhooks double-write.
  The guard needs to move above ledger.record()."
